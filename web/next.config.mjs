/** @type {import('next').NextConfig} */
const nextConfig = {
  // Emit a self-contained server bundle in .next/standalone for packaged deploys.
  output: 'standalone',
};

export default nextConfig;
